pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Code downloaded'
            }
        }

        stage('Build') {
            steps {
                echo 'Application Build'
            }
        }
    }
}
